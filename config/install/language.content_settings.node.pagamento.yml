langcode: it
status: true
dependencies:
  config:
    - node.type.pagamento
id: node.pagamento
target_entity_type_id: node
target_bundle: pagamento
default_langcode: site_default
language_alterable: false

